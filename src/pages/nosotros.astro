---
import Layout from "@layouts/Layout.astro";
import Container from "@components/container.astro";
import { Image } from "@astrojs/image/components";
import { authors } from "@data/authors";
import Pagetitle from "@components/pagetitle.astro";
import Link from "@components/ui/link.astro";
---

<Layout title="Nosotros">
  <Container>
    <Pagetitle>
      <Fragment slot="title">Nosotros</Fragment>
      <Fragment slot="desc">¡Conoce nuestro equipo!.</Fragment>
    </Pagetitle>

    <div class="grid md:grid-cols-3 gap-10 mx-auto max-w-4xl mt-12">
      {
        authors.slice(0, 3).map((item) => (
          <div class="group">
            <div class="w-full aspect-square">
              <Image
                src={item.image}
                format="avif"
                width={580}
                height={580}
                alt="Team"
                class="w-full h-full object-cover rounded transition  group-hover:-translate-y-1 group-hover:shadow-xl"
              />
            </div>

            <div class="mt-4 text-center">
              <h3 class="text-sm text-gray-500 dark:text-gray-300">
                {" "}
                {item.name}
              </h3>
            </div>
          </div>
        ))
      }
    </div>

    <div class="mx-auto max-w-xl text-center mt-14">
      <p class="text-lg leading-relaxed text-gray-600 dark:text-gray-400">
        Nuestra empresa se destaca por la pasión por la excelencia y la innovación. Con un equipo capacitado y una sólida trayectoria, lideramos el mercado con integridad y profesionalismo, trabajando para superar expectativas y crear valor para nuestros clientes. Confíe en nosotros para llevar su visión a la realidad y experimentar la diferencia.
      </p>
      <div class="mt-8">
        <Link href="/contacto" style="primary">Contactanos</Link>
      </div>
    </div>
  </Container>
</Layout>
